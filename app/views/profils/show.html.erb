<h1>Ton Profil</h1>


          <ul>
            <h1><%= @user.nickname %></h3>
<%= cl_image_tag @profil.img_profil.key, class: "profil_avatar", gravity: :face %>
            <h2><%= @profil.nom.capitalize %></h1>
            <h2><%= @profil.prenom.capitalize %></h2>
            <h3><%= @profil.age %>ans</h3>
            <h3><%= @user.email %></h3>
            <h3><%= @profil.email %></h3>

          </ul>


<h1>Mes Planches en Vente</h1>
    <% @items.each do |item| %>
            <ul style="list-style-type:none; padding-left:0px; margin-bottom:0px;">
            <h1><%= item.marque.capitalize %></h1>
            <h2><%= item.taille %></h2>
            <h3><%= item.price %>€</h3>
<%= link_to 'Modifier', edit_item_path(@item) %>
          </ul>
        </div>
      </div>
      <% end %>



  <div class="bouton-add">
    <%= link_to'Retour', profils_path %>
    <%# <%= link_to "Modifie ton profil", edit_profil_path(current_user.profil) %> %>

  </div>
